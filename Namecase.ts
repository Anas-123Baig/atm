// NameFormat.ts

let name: string = "Eric";

console.log(name.toLowerCase()); // prints the name in lowercase
console.log(name.toUpperCase()); // prints the name in uppercase
console.log(name.charAt(0).toUpperCase() + name.slice(1)); // prints the name in titlecase
